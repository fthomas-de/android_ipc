<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.broadcast.unibremen.broadcastreceiver" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <receiver
            android:name="com.example.root.broadcastreceiver3.MyReceiver"
            android:enabled="true"
            android:exported="true" >

            <intent-filter>
                <action android:name="_myAction_" >
                </action>
                <category android:name="_myCategory_" />
                <data android:scheme="something" />
                <data android:host="something" />
                <data android:port="something" />
                <data android:path="something" />
                <data android:pathPrefix="something" />
                <data android:pathPattern="something" />
                <data android:mimeType="image/png" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
